name: CI/CD React → GHCR → k3s

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  packages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18

      - run: |
          npm ci || npm install
          npm run build

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - run: |
          IMAGE="ghcr.io/${{ github.repository }}:latest"
          docker build -t "$IMAGE" ./app
          docker push "$IMAGE"

      # - uses: azure/setup-kubectl@v4

      # - run: |
      #     mkdir -p ~/.kube
      #     echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config

      # - run: |
      #     OWNER="${{ github.repository_owner }}"
      #     REPO="$(echo "${{ github.repository }}" | cut -d'/' -f2)"
      #     sed "s|\\${GITHUB_OWNER}|$OWNER|g; s|\\${GITHUB_REPO}|$REPO|g" \
      #       k8s/deployment.yaml > k8s/deployment.rendered.yaml
      #     kubectl apply -f k8s/deployment.rendered.yaml
      #     kubectl rollout status deployment/react-demo --timeout=120s

      # - run: kubectl get svc react-demo-svc -o wide
